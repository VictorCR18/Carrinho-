import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#003263",
      contrastText: "#fff",
    },
    secondary: {
      main: "#fa830c",
      contrastText: "#fff",
    },
  },
});
